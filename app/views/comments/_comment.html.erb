<%= div_for(comment) do %>
  <blockquote class="blockquote-reverse">
    <%= simple_format(comment.text) %>
    <footer>
      <%= comment.user %>
    </footer>
    State: <%= state_for(comment) %>
  </blockquote>
<% end %>
